#include "main.h"

/**
 * _isdigit - checks that a given character is a digits
 * @c: parameter given in the function
 * Return: excutes on sucess
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

#include "main.h"

/**
 * _isupper - function that check that a character is an uppercase
 * @c: parameter to function
 * Return: on sucess return 0
 */
int _isupper(int c);
{
	return (c >= 'A' && c <= 'Z');
}

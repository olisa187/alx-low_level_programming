#include <stdio.h>

/**
 * main - loop
 * display single digits
 * Return: 0
 */
int main(void)
{
	int num
		;
	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
